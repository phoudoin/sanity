* Convert input value according unit:
	1000 µs -> 1 ms
	32 bits -> 4 bytes
	8192 bits -> 1 Kb
	25,4 mm -> 2,54 cm -> 1 inch 
	10 mm -> 1 cm
	-> DONE, except for metric system -> inch & co

* Remove trailing ".00" from decimal values when Quant (if any) is integer also...

* CurveControl, supporting int min, int max, nb values, a label, few curves modes (gamma, segments, splines/bezies + reset)

* SpinControl 
	Add support to input decimal value, not only integer
        Fix label placement bug

* Group all single options in a single option group into the same generic CollapsableBox -> niash driver, for example

* Add icons to resource file
	-> DONE, easy to add more...

* Add a CollapsableBox with driver infos
	-> DONE

* Add Preview + scan area(s) selection
	-> DONE

* Fix negative SANE_Fixed bug with slider (which interpret it as unsigned integer!!!)

* Rework visual interface more globally:
  - tiny preset MenuField
  - Load/save settings (options values, window position, CollapsableBoxes state, etc)
  - Prescan button, progress bar to better integrate *somewhere* else



